// App.js: Main application component
// Renders the Accordion component inside a container div
import Accordion from "./Accordion";

function App() {
  // Render the Accordion component
  return (
    <div>
      <Accordion />
    </div>
  );
}

// Export the App component as default
export default App;
