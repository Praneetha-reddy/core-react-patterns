/**
 * Main
 * Layout wrapper for the main application content.
 * Props:
 * - children: React nodes rendered inside the main element.
 */
export default function Main({ children }) {
  return <main className="main">{children}</main>;
}
